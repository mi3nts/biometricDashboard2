
In order to view: 
1) Simply open index.html in browser


In order to run and edit using SASS:
1) Install node modules
2) npm run init
4) Add node script from package.json
5) npm install node-sass -D
